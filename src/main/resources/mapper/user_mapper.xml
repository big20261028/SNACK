<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.user">
	<select id="dupcheck" parameterType="com.ezen.vo.UserVO" 
		resultType="java.lang.Integer">
		select count(*) 
		from user 
		where userid = #{userid} 
		and email = #{email}
	</select>
	<select id="idDupCheck" parameterType="java.lang.String" 
		resultType="java.lang.Integer">
		select count(*) 
		from user 
		where userid = #{userid} 
	</select>
	<select id="emailDupCheck" parameterType="java.lang.String" 
		resultType="java.lang.Integer">
		select count(*) 
		from user 
		where email = #{email}
	</select>
	<select id="usercheck" parameterType="java.lang.Integer" 
		resultType="java.lang.Integer">
		select count(*) 
		from user
		where usernum = #{usernum}
		and status = 'Y'
	</select>
	<select id="ispwcol" parameterType="com.ezen.vo.UserVO" 
		resultType="java.lang.Integer">
		select count(*) 
		from user 
		where usernum = #{usernum} and userpw = md5(#{userpw})
	</select>
	
	<insert id="join" parameterType="com.ezen.vo.UserVO">
		insert into user (userid, email, userpw, name, gender) 
		values(#{userid}, #{email}, md5(#{userpw}), #{name}, #{gender}) 
	</insert>
	
	<select id="status" parameterType="java.lang.String" resultType="java.lang.String">
		select status from user where userid=#{userid}
	</select>
	
	<select id="login" parameterType="com.ezen.vo.UserVO"
		resultType="com.ezen.vo.UserVO">
		select usernum,userid,email,status,intro,isadmin,profileimgp,profileimgf,
		(select count(*) from follow where fromnum = user.usernum) as followCnt,
		(select count(*) from follow where tonum = user.usernum) as followerCnt
		from user
		where userid=#{userid} and userpw = md5(#{userpw})
	</select>
	
	<update id="intro" parameterType="com.ezen.vo.UserVO">
		update user set intro = #{intro} where usernum = #{usernum}
	</update>
	
	<update id="emailupdate" parameterType="com.ezen.vo.UserVO">
		update user set email = #{email} where usernum = #{usernum}
	</update>
	
	<update id="userupdate" parameterType="com.ezen.vo.UserVO">
		update user set userpw = md5(#{userpw}) where usernum = #{usernum}
	</update>
	
	<select id="isAdmin" parameterType="java.lang.Integer" resultType="java.lang.Integer">
		select count(*) from user
		where usernum = #{usernum} and isadmin = 'Y'
	</select>
	
	<select id="userInfo" parameterType="java.lang.Integer" resultType="com.ezen.vo.UserVO">
		select usernum, userid, intro, profileimgp, profileimgf,
		(select count(*) from follow where fromnum = #{usernum}) as followCnt,
		(select count(*) from follow where tonum = #{usernum}) as followerCnt
		from user
		where usernum = #{usernum}
	</select>
	
	<select id="getTotal" parameterType="com.ezen.vo.SearchVO" resultType="java.lang.Integer">
		select count(*) from user where 1 = 1 
		<if test="!userStatus.equals('')">
			and status = #{userStatus}
		</if>
		<if test='keyword != null  and !keyword.equals("")'>
			<if test="userSearchFilter == 1">
				and userid like '%${keyword}%'
			</if>
			<if test="userSearchFilter == 2">
				and name like '%${keyword}%'
			</if>
			<if test="userSearchFilter == 3">
				and email like '%${keyword}%'
			</if>
		</if>	
	</select>
	
	<select id="userList" parameterType="com.ezen.vo.SearchVO" resultType="com.ezen.vo.UserVO">
		select usernum, userid, name, gender, email, status
		from user where 1 = 1 
		<if test="!userStatus.equals('')">
			and status = #{userStatus}
		</if>
		<if test='keyword != null  and !keyword.equals("")'>
			<if test="userSearchFilter == 1">
				and userid like '%${keyword}%'
			</if>
			<if test="userSearchFilter == 2">
				and name like '%${keyword}%'
			</if>
			<if test="userSearchFilter == 3">
				and email like '%${keyword}%'
			</if>
		</if>	
		order by usernum desc
		limit #{offset},10	
	</select>
	
	<update id="statusY" parameterType="java.lang.Integer">
		update user set status = 'Y'
		where usernum = #{usernum}
	</update>
	<update id="statusN" parameterType="java.lang.Integer">
		update user set status = 'N'
		where usernum = #{usernum}
	</update>
	<update id="statusB" parameterType="java.lang.Integer">
		update user set status = 'B'
		where usernum = #{usernum}
	</update>
	
	<select id="adminUserInfo" parameterType="java.lang.Integer" resultType="com.ezen.vo.UserVO">
		select * from user
		where usernum = #{usernum}
	</select>
	
	<update id="adminUserModify" parameterType="com.ezen.vo.UserVO">
		update user set
		status = #{status},
		isadmin = #{isadmin},
		name = #{name},
		gender = #{gender},
		email = #{email},
		intro = #{intro}
		<if test="userpw != null and !userpw.equals('')">
			, userpw = md5(#{userpw})
		</if>
		where usernum = #{usernum}
	</update>
	
	<select id="searchUserid" parameterType="java.lang.String" resultType="java.lang.String">
		select userid from user where email = #{email}
	</select>
	
	<update id="pwUpdate" parameterType="com.ezen.vo.UserVO">
		update user 
		set userpw = md5(#{userpw})
		where userid = #{userid} and email = #{email}
	</update>
	
	<update id="updateProfileImg" parameterType="com.ezen.vo.UserVO">
		update user
		set profileimgp = #{profileimgp}, profileimgf = #{profileimgp}
		where usernum = #{usernum}
	</update>
</mapper>